<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body><br>
  <h1>affichage current profil</h1>
      <%if @current_user.avatar.attached?%>
        <%= image_tag @current_user.avatar, alt: 'avatar' %>
      <%else%>
        <p>=== Il n'y a pas encore d'avatar lié à cet utilisateur ===</p>
      <%end%><br><br>
      <h3>Changer d'avatar ?</h3>
      <%= form_tag avatars_path(@current_user), multipart: true do %>
        <%= file_field_tag :avatar %>
        <%= submit_tag "mettre à jour" %>
      <% end %><br><br>

    ton email est :
    <%= current_user.email %><br>
    ton prénom est :
    <%= current_user.first_name %><br>
    ton nom est :
    <%= current_user.last_name %><br>
    A propos :
    <%= current_user.description %><br><br>
    les events que tu as créer :

    <%@event.each do |e|%><br>
      <%=e.title%>
    <%end%>
    <br><br>
    <%=link_to "edit profil", edit_user_path%><br>
    <%=link_to "Back", event_index_path%>
  </body>
</html>
